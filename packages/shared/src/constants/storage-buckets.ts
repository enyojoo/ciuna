/** Hub catalog uploads (products, vendors, experts, service line icons). Office uses `office/lib/upload-hub-assets.ts`. */
export const HUB_ASSETS_BUCKET = "hub-assets" as const

/** Payment method QR uploads (Office settings only). Must not be used for hub catalog images. */
export const PAYMENT_QR_CODES_BUCKET = "payment-qr-codes" as const
