{
  "name": "ciuna",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.9.3",
  "workspaces": [
    "packages/*",
    "web",
    "office"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=web",
    "dev:web": "turbo run dev --filter=web",
    "dev:all": "turbo run dev --filter=web --filter=office",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=web",
    "build:shared": "turbo run build --filter=@ciuna/shared",
    "lint": "turbo run lint",
    "start:web": "turbo run start --filter=web",
    "dev:office": "turbo run dev --filter=office",
    "build:office": "turbo run build --filter=office",
    "start:office": "turbo run start --filter=office"
  },
  "devDependencies": {
    "turbo": "^2.8.12"
  },
  "dependencies": {
    "next": "^16.1.6"
  }
}
